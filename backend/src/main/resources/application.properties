# ============================================================
# SERVER
# ============================================================
server.port=8080

# ============================================================
# DATABASE (MySQL)
# Set these as environment variables or replace values below
# ============================================================
spring.datasource.url=jdbc:mysql://${DB_HOST:nozomi.proxy.rlwy.net}:${DB_PORT:31971}/${DB_NAME:railway}?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:kqePwLNAsBdOoEYTchYrBouyCFgulIOp}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ============================================================
# JPA / HIBERNATE
# ============================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ============================================================
# TELEGRAM BOT
# Set TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID as env variables
# ============================================================
telegram.bot.token=${TELEGRAM_BOT_TOKEN:8621012337:AAEJdEYR5F0SO5o9QdzxOmABeV_YBMt6WiQ}
telegram.chat.id=${TELEGRAM_CHAT_ID:2107433797}

# ============================================================
# ASYNC THREAD POOL
# ============================================================
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=20
spring.task.execution.pool.queue-capacity=200
spring.task.execution.thread-name-prefix=telegram-async-

# ============================================================
# LOGGING
# ============================================================
logging.level.com.telegramapproval=DEBUG
logging.level.org.hibernate.SQL=DEBUG
